const message = "Hello World";
let count = 0;

function increment() {
    count = count + 1;
    return count;
}

function greet(name) {
    return "Hello, " + name + "!";
}

const numbers = [1, 2, 3, 4, 5];
const user = {
    name: "John",
    age: 30
};